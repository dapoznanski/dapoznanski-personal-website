import React from "react";
import { Container, Typography, Button, Box } from "@mui/material";

const CV = () => {
    return (
        <Container>
            <h1>CV Page</h1>
        </Container>
    );
};

export default CV;
